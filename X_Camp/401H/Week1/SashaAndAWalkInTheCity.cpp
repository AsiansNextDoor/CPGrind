#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

#define IOS ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define endl "\n"
#define f first
#define s second

vector<int> dpv;
vector<int> visited;
int dfs(int u) {

}

signed main() {
    IOS;

    int t; cin >> t;
    while (t--) {
        int n; cin >> n;
        dpv.clear();
        dpv.resize(n+1);
        visited.clear();
        visited.resize(n+1);
    }
}